# Authentication

JWT

Access Token

Refresh Token

bcrypt password hashing

Auth module owns authentication.

Users module owns user management.

Never store passwords.

Always validate credentials.

Use Guards.

Use Passport.